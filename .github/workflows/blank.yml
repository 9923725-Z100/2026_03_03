name: Hello World

on:
  push:
  pull_request:

jobs:
  hello:
    timeout-minutes: 1
    runs-on: [self-hosted, TISA]
    steps:
      - name: Echo Hello World
        shell: cmd
        run: echo "helloworld"
